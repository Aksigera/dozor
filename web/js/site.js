window.onload = function(){
     var timerCountdown = document.getElementsByClassName('timer--countdown')[0];
    console.log(timerCountdown.innerHTML);
    var remainingSeconds = timerCountdown.innerHTML;
    setInterval(function(){
        timerCountdown.innerHTML=
    })
}